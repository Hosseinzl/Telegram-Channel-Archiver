services:
  channel-archiver:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: faraz-channel-archiver
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./session_data:/data
    networks:
      - archiver-net


networks:
  archiver-net:
    external: true